//console.log(a); // Error

console.log(b);

let a=10;
var b=20;

//the time at which the variable gets memory but not initialized with a value .that time is known as TDZ.
//not only the var, even the let and const are hosted. but they will be in TDZ, thats why the error comes if we apply hoisting to let,const.